{
  "name": "ai-cs-mvp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "cd frontend && npm start",
    "install-all": "npm install && cd frontend && npm install && cd ../api && npm install"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
